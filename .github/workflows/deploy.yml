name: Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    uses: bborchers/ha-addons-workflow/.github/workflows/build-deploy.yml@main
    with:
      addon-path: uptimekuma
      slug: uptimekuma
    secrets:
      dispatch-token: ${{ secrets.DISPATCH_TOKEN }}
    permissions:
      contents: read
      packages: write
